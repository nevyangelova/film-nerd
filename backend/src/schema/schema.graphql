type Query {
    helloWorld: String!
    fakeMovie: Movie!
    movie(title: String!): Movie!
}

type Movie {
    Title: String!
    Year: String!
    Director: String!
    Writer: String!
    Actors: [String!]!
}
